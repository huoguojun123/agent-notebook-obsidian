{
  "name": "agent-notebook-obsidian-plugin",
  "version": "0.2.2",
  "private": true,
  "description": "Obsidian plugin for notebook-scoped opencode prompt workflows.",
  "main": "main.js",
  "scripts": {
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs",
    "dev": "node esbuild.config.mjs --watch"
  },
  "keywords": [
    "obsidian",
    "plugin",
    "agent",
    "notebook"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^24.10.1",
    "builtin-modules": "^5.0.0",
    "esbuild": "^0.27.0",
    "obsidian": "^1.8.7",
    "typescript": "^5.9.3"
  }
}
